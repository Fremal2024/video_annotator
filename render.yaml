services:
  # ============================================================
  # BACKEND — Django Web Service
  # ============================================================
  - type: web
    name: video-annotator-backend
    runtime: python
    plan: free
    buildCommand: "./build.sh"
    startCommand: "gunicorn video_annotation.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.0"
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "*"
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: video-annotator-db
          property: connectionString
      - key: GEMINI_API_KEY
        sync: false
      - key: CORS_ALLOW_ALL_ORIGINS
        value: "True"

  # ============================================================
  # FRONTEND — React Static Site
  # ============================================================
  - type: web                  
    name: video-annotator-frontend
    runtime: static            
    rootDir: frontend
    buildCommand: "npm install && npm run build"
    staticPublishPath: build   
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: REACT_APP_API_URL
        value: "https://video-annotator-backend.onrender.com"

databases:
  - name: video-annotator-db
    plan: free
    databaseName: video_annotator
    user: video_user